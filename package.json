{
  "name": "test",
  "version": "0.1.0",
  "private": true,
  "description": "test",
  "devDependencies": {
    "autoprefixer": "^6.5.3",
    "browser-sync": "^2.3.1",
    "del": "^2.2.2",
    "gulp": "^4.0.0",
    "gulp-plumber": "1.1.0",
    "gulp-rename": "^1.2.2",
    "run-sequence": "^1.2.2"
  },
  "scripts": {
    "test": "editorconfig-cli",
    "build": "gulp build",
    "start": "npm run build && gulp serve"
  },
  "editorconfig-cli": [
    "*.html",
    "*.json",
    "*.js",
    "js/**/*.js",
    "img/**/*.svg",
    "sass/**/*.{sass,scss}"
  ],
  "engines": {
    "node": "10.1"
  }
}
